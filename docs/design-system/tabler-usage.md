# Tabler Usage

Use Tabler for layout, cards, alerts, tables, form controls, and responsive grid behavior. Seeder primitives should render Tabler-compatible markup instead of inventing parallel component classes.
