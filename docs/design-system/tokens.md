# Tokens

Project tokens live in `public/assets/css/seeder-tokens.css`.

Use these variables for seeder-specific spacing, borders, surfaces, and focus states. Keep Tabler as the base design system and use seeder tokens only where the template needs a stable convention.
