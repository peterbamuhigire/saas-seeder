# Icon Standard

Seeder menu items store icon names as stable identifiers. Current rendered includes still use text-first Tabler navigation. Future visual icon rendering should use one consistent source per project and must not mix placeholder links with icons.
