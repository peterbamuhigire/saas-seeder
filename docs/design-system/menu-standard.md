# Menu Standard

Menus should be defined as data via `MenuItem` and rendered with `MenuRenderer`.

Menu entries require:

- stable route,
- label,
- panel,
- module code where applicable,
- permission code where applicable,
- active route pattern.

Do not add `href="#"` placeholders.
