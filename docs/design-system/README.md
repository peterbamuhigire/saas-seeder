# Design System

The SaaS Seeder UI layer builds on Tabler and adds a small set of project primitives for shell layout, menus, forms, and common states.

Reference examples are available at `/public/ui-examples/`.
